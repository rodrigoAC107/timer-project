export interface countdownInitial {
  hours: string | number;
  minutes: string | number;
  seconds: string | number;
}

export const initialCountDown: countdownInitial = {
  hours: "00",
  minutes: "00",
  seconds: "00",
};
